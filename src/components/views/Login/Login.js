import React from "react";
import styles from "./Login.scss";
import Typography from "@material-ui/core/Typography";

const Login = () => {
  return (
    <h2 className={styles.component}>
      <Typography variant="h3">Login View</Typography>
    </h2>
  );
};

export default Login;
